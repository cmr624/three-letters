export function getFirstCharacter(string : string) : string {
    return string.charAt(0);
}

export function getLastCharacter(string : string) : string {
    return string.charAt(string.length - 1);
}
